namespace AOOP_HABI.Models
{
    public enum Intent { Build, Break }
    public enum TrackingType { Binary, Quantitative }
    public enum HabitCategory { Popular, Health, Sports, Lifestyle, Academic, Quit, Time }
    public enum GoalPeriod { Daily, Weekly, Monthly }
    public enum TimeRange { Anytime, Morning, Afternoon, Evening }

}
